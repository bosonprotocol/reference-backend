apiVersion: v1
kind: Service
metadata:
  name: {{ include "reference-backend.fullname" . }}
  labels:
    {{- include "reference-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reference-backend.selectorLabels" . | nindent 4 }}
